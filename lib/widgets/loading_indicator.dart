import 'package:flutter/material.dart';

class LoadingIndicatorRapli extends StatelessWidget {
  const LoadingIndicatorRapli({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
